# 宋词声景：未经验证的 AI 拟音原型

> **状态：AI 辅助 vibe coding 原型，未经验证。** 本项目的代码、页面、IPA、宋词韵部、临安实现和合成声音，均没有经过历史语言学、方言史、语音学或听辨实验的正式验证。它只能作为可检查的探索工具，**不能**作为宋代文人通语、南宋临安真实口音、宋词用韵结论或教学／引用依据。

这是一个浏览器内运行的宋词与古文朗读实验。默认入口是“宋代文人通语”，其次是尚未完成地域改音的“南宋临安实现”；婺州／金华已降为南部吴语比较资料，不再是主声景。页面按输入、韵脚、逐字拟音、试听的顺序组织。发布产物是可直接放到 CDN 的静态 SPA：核心功能不需要 Node、MLX、Python 或推理服务。Sites 构建中的 Worker 仅转发静态资源与 SPA 回退，不参与分析或合成。可选的“神经代理”会在用户首次启用时从公网下载模型，并在浏览器本地推理。

## 目录

- [当前能做什么](#当前能做什么)
- [本地启动](#本地启动)
- [系统边界](#系统边界)
- [已知问题与可能的解决方案](#已知问题与可能的解决方案)
- [声音改进的取舍](#声音改进的取舍)
- [入声的现代方言对照](#入声的现代方言对照)
- [资料、版本与贡献](#资料版本与贡献)

## 当前能做什么

- 用 `tshet-uinh` 查询《广韵》系音韵地位、反切和释义。
- 显示白一平转写、北宋桥接 IPA 和逐层置信等级；默认通语标 B，临安实验层标 C。
- 先显示韵脚，再显示逐字 IPA；现有平水 106 韵仅作官韵／格律诗**提示**，尚未实现宋词通语韵部。
- 允许对单字读音选择候选项，再导出注释 JSON。
- 在浏览器用规则 DSP 渲染女声风格的研究声音，或用设备自己的中文声线预听。
- 按需加载 Kokoro 中文女声，作为“接近真人”的现代普通话音质参照；它不读取拟音 IPA，也不是历史拟音。

这不是一个经过验证的“古音 TTS”。48 kHz 只是渲染格式；声音清晰度、自然度和历史可信度是彼此独立的问题。

## 本地启动

需要 Node.js **22.13 或更高版本**。

```bash
npm install
npm run dev
```

打开终端给出的本地地址（通常为 `http://localhost:3000`）。静态发布文件会生成到 `dist/`，将该目录内容上传至支持 SPA 回退到 `index.html` 的 CDN／静态站点即可。运行工程检查：

```bash
npm run test:research
npm run lint
npm run build
```

这些检查只验证数据路径、可重复渲染和前端构建；**不验证拟音或听感是否正确**。

## 系统边界

```text
汉字文本
  → 繁简转换、断句与少量上下文规则
  → 《广韵》系音韵地位
  → 宋人音注／宋词用韵（待编码）与北宋桥接
  → 宋代文人通语基线
  → 南宋临安接触层（可选、待研究）
  → IPA、官韵参考与证据标签
  → IPA 可控规则 DSP／现代普通话神经代理／设备 TTS
```

每一层都必须单独评价：韵书条目的可靠性，并不会自动证明宋代音值、地方口语或最终合成声音可靠。

## 当前研究主轴

宋词作者来自多地，但现有用韵研究显示江浙词人的主体韵系与中州、山东、江西、福建和四川大体相同，因此产品先建“宋代文人通语”，再标稳定的地域例外。南宋临安是通语之上的城市接触实现，不是另一套凭现代杭州话倒推的完整音表。婺州／金华材料只用于检验南部吴语古层、文白异读和入声演变。资料入口与证据边界见 [DATA_SOURCES.md](DATA_SOURCES.md)。

## 已知问题与可能的解决方案

| 问题 | 目前状态 | 可行的下一步 |
| --- | --- | --- |
| 《广韵》不等于宋代文人通语 | 字头与音韵地位较可追溯；当前 `n_song` 只是北宋桥接 | 逐类加入宋人音注、宋代韵图和宋词实际用韵；对争议音变并列方案 |
| 宋词韵部尚未实现 | 页面仍以平水 106 韵作参考，不足以裁决词牌与通押 | 建立约 18 部的宋词通语模型及“作者—作品—年代—韵段—韵脚”回归语料 |
| 临安音值证据不足 | 临安方案当前与通语基线同音，只改变证据标签 | 比较老派杭州城区、外围吴语、宋音材料与人口史；只对有证据的音类启用规则 |
| 婺州不再是主目标 | 金华资料过去与临安平级展示，但没有形成独立规则 | 保留为南部吴语古层、文白异读和入声演变的比较材料；只有明确婺州作品才恢复独立实验入口 |
| 多音、通假、异文与古文分词 | 仅覆盖少量上下文规则，默认候选仍可能错 | 引入带版本的古籍底本、词级读音词典、作者／时代元数据和人工覆写保存；将“无法断定”作为正式输出 |
| 诗词押韵被过度简化 | 平水 106 韵只适合提示一部分格律诗，宋词、古体、通押与 107 韵尚未完成 | 建立作品级“作者—文本—韵段—韵脚”语料；分别编码《礼部韵略》106／107 韵与词牌规则；用校勘本人工回归 |
| 声母仍可能不清晰 | v6 提高擦音、塞擦音、爆破与送气段的相对能量，并把 `/p t k/` 爆破频谱次序写入回归；工程指标改善但尚未做听辨实验 | 制作最小对立对（如 `pa/ta/ka`、`tsa/tʂa/tɕa`）听辨测试；按混淆矩阵调时值、噪声频段和共振峰；保存盲测结果而不只保存波形指标 |
| “48 kHz 仍粗糙” | 采样率不是主要瓶颈；v6 用声门流脉冲＋串联共振器取代逐谐波正弦叠加，并把元音气声降至 `0.0012`。固定样本的稳态相对粗糙度由 `0.123` 降到 `0.119` | 保留 DSP 作为可控校音器；下一阶段应试验真正接受 IPA／音素及时长控制的浏览器声学模型／声码器，并以 ABX／MOS 验证听感 |
| 声音不像真人 | 新增 Kokoro 中文女声作自然度基准；它按现代普通话文本发音，不接受本项目的南宋 IPA | 不能把代理声线冒充历史拟音。若要兼得自然度与目标音系，需要许可证明确、覆盖目标音素的授权录音及声学模型适配 |
| 神经代理首次加载较慢 | WebGPU 路径约下载 166 MB 模型，WASM 回退约 127 MB，另有运行时与约 522 kB 声线文件；依赖 jsDelivr 与 Hugging Face 可用 | 继续保持主动加载、进度提示和回退；若部署预算允许，可自行托管并做长期缓存，但仍须保留第三方许可证 |
| 单字急促、入声不突出 | v5 将舒声基础时值从 420 ms 增至 630 ms，入声从 260 ms 增至 390 ms；v6 保留该比例和普通字间 40 ms 空白 | 入声另以提前闭锁、14 ms 末尾静音和无释放 `/p t k/` 收尾区分；具体比例仍需现代方言语料与听辨校准 |
| 平、上、去仍不易分辨；标点节奏不一致 | v7 将平声设为高平、上声设为先抑后显著扬、去声设为高起陡降，入声保持短促闭锁；每枚 Unicode 标点静音一个普通舒声音节，连续标点逐枚累计 | 当前 F0 只是增强可听差异的 D 级工程参数；需用最小对立调听辨实验校准，不能因为更容易听出就提高南宋调值的历史置信度 |
| 听起来对不代表历史上对 | 目前没有专家听辨和文献交叉验证闭环 | 把“文献一致性”“规则一致性”“专家判断”“听辨可懂度”设为独立验收项，公开反例、改动记录和不确定项 |

## 声音改进的取舍

v7 的声调调整参考现代多调方言的声学建模方法：声调辨识不能只靠几条相近直线，还要同时拉开基频音域、运动方向、起止目标和轮廓拐点。苏州／吴江研究显示同一音节内可需要一至三个不同极性、时点和幅度的调形命令；本项目只采用这条可听性原则，没有把其七调／十二调调值映射回南宋。每枚标点的静音按当前语速换算为一个普通舒声音节，连续标点会累计。

当前 DSP 的目标只是让声母不再全部退化为相似的元音。v6 参考经典的声源—滤波器思路，以非对称声门流脉冲激励四级时变共振器；辅音仍用分频噪声、闭塞、爆破、送气和共振峰过渡独立生成。元音与辅音轨分别限幅和校准，避免强爆破把整字元音压成耳语；入声塞尾的共振峰移动也限制幅度并同步衰减，避免闭锁前出现异常尖峰。这是紧凑的工程实现，并非完整复刻 Klatt 合成器，更**不应被误读为南宋声学事实**。

在固定机器上的自动 A/B 中，20 字样本的同步渲染约从 `2.88 s` 降至 `0.28 s`；擦音／塞擦音起始段 RMS 约提高三成，稳态元音相对一阶差分 RMS 从 `0.123` 降至 `0.119`。回归还检查双唇—软腭—齿龈塞音爆破频谱中心依次升高、送气段与不送气闭锁分离、入声末尾保持全零。它们只是可复现的工程代理指标，不等于“人耳必然更清楚”。理论入口为 Klatt 的[串联／并联共振峰合成器论文](https://doi.org/10.1121/1.383940)。

如果未来要追求“清晰且接近真人”的声音，优先顺序建议是：

1. 先做最小对立对听辨与长句可懂度测试，找出真正混淆的声母／韵尾。
2. 保留 IPA、音素时长、入声闭锁和韵脚控制的接口，避免通用 TTS 擅自改写音系。
3. 继续调研可在浏览器运行且能接受所需音素的 WASM／ONNX 路线；当前 Kokoro 代理已经证明浏览器神经合成可行，但只会读现代普通话。
4. 只有在授权数据、训练方案、听辨测试和专家审阅都齐备后，才把神经女声作为默认输出。

## 可选神经代理的外部资源

- 模型：`onnx-community/Kokoro-82M-v1.1-zh-ONNX`，Apache-2.0；页面根据能力选用 q4f16／q8，并固定女性声线 `zf_001`。
- 浏览器封装：`@uzen/kokoro-js@1.2.4`，Apache-2.0；从固定版本的 jsDelivr ES module 延迟载入，WebGPU 失败时回退 WASM。
- 模型与声线在 Hugging Face 延迟下载并由浏览器缓存。核心研究合成、音韵分析和静态页面不依赖这些公网资源。
- 没有加入 Piper：当前引擎为 GPL-3.0，公开中文声音的数据许可也需逐个核验；例如 Xiao Ya／BZNSYP 声音注明非商业数据许可，暂不适合作为本项目默认可再分发资源。

外部 URL、版本或托管方失效时，神经代理可能不可用，但不会影响核心静态页面和确定性研究合成。

## 入声的现代方言对照

v5／v6 没有把现代苏州话或粤语直接当成南宋音值，而只抽取两类可验证的比较线索：现代苏州入声通常较短，并常以喉塞或元音末端喉化实现；粤语入声保留不除阻的 `/p t k/` 韵尾，音节也短于舒声。当前实现保留宋代桥接 IPA 已给出的 `/p t k/` 部位，提前向相应闭锁过渡，末尾 14 ms 完全静音且不添加释放爆破。

可用于下一轮离线测量的公开资源包括：[WenetSpeech-Wu](https://github.com/ASLP-lab/WenetSpeech-Wu-Repo) 及其苏州话 benchmark、[Mozilla Common Voice 粤语](https://commonvoice.mozilla.org/data)、[WenetSpeech-Yue](https://github.com/ASLP-lab/WenetSpeech-Yue)，以及仅 10 分钟但带 48 kHz 女声录音的 [MagicData 苏州话 TTS Lite](https://huggingface.co/datasets/MagicHub/magicdata-dialect-wu-chinese-tts-lite)。最后一项正文许可为 CC BY-NC-ND 4.0，因此本项目不打包、不改编其音频，只把它登记为非商业研究对照。

## 资料、版本与贡献

- 资料和许可证登记见 [DATA_SOURCES.md](DATA_SOURCES.md)。
- 对拟音与声音的专项审查见 [REVIEW.md](REVIEW.md)。
- 产品边界和理想验收标准见 [GOAL.md](GOAL.md)。

贡献时请把“资料事实”“学术推断”“合成策略”分开记录；任何新规则都应带来源、适用时间／地域、置信等级、反例和回归测试。不要因为声音更顺耳，就提高历史结论的置信度。
